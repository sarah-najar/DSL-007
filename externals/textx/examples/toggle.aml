application toggle
sensor button:9
actuator led:12
buzzer bz:8

state off {
    set led to LOW
    when button is HIGH goto on
} initial

state on {
    set led to HIGH
    shortBeep bz
    when button is HIGH goto off
}
